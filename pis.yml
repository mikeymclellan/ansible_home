---
# pis playbook: define all the roles for pis

- hosts: pis
  remote_user: pi
  sudo: yes

  vars:
    - wificlient_networks:
       - ssid: McLellan
         psk: minceontoast

  roles:
    - common
    - wificlient


  handlers:
    - include: handlers/handlers.yml
